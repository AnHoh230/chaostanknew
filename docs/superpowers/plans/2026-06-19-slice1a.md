# ChaosTankNew Slice 1a — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Das disziplinierte Engine-Fundament plus einen fahrenden, schießenden, modular zusammengesetzten Panzer in einer 3D-Welt bauen — das beweist die Anti-ChaosTank-Architektur, bevor (in Slice 1b) die lebenden Gegner-Systeme daraufkommen.

**Architecture:** Reine TypeScript-Logikmodule (mit Vitest unit-getestet) + eine dünne Babylon-Integrationsschicht (über den Dev-Server verifiziert). **Genau ein** Game-Loop, getrieben von einer `simSpeed`-Clock; typisierter Event-Bus; seedbarer RNG; gestuftes Logging; gepoolte **kinematische** Projektile mit explizitem Zustandsautomaten (keine Phantomschüsse). Spec-Bezug: `docs/superpowers/specs/2026-06-18-chaostank-neu-design.md`, Abschnitte 2–5, 17–18, 21.

**Tech Stack:** Vite · TypeScript (strict, ESM) · Babylon.js v8 (`@babylonjs/core`) · Vitest.

---

## Fundament: Dateistruktur & Verantwortlichkeiten

Alle Pfade relativ zur Projektwurzel `ChaosTankNew/`.

| Datei | Verantwortung | Art |
|---|---|---|
| `package.json`, `tsconfig.json`, `vite.config.ts`, `index.html` | Gerüst, Build, Test-Runner, Canvas | Config |
| `src/main.ts` | Bootstrap: Start-Screen → instanziiert alles, startet **einen** Loop | Integration |
| `src/core/rng.ts` | seedbarer RNG (mulberry32) | Logik (TDD) |
| `src/core/log.ts` | gestuftes Logging (Level + Kanal + Gate), kein stiller Master-Mute | Logik (TDD) |
| `src/core/clock.ts` | `simSpeed`-Clock → `simDt` in Sekunden | Logik (TDD) |
| `src/core/events.ts` | typisierter Event-Bus (Payload direkt, kein Wrapping) | Logik (TDD) |
| `src/core/loop.ts` | genau ein Render-/Update-Loop | Integration |
| `src/world/biomeRegistry.ts` | Biom-Registry (Single Source), `getBiome` wirft bei unbekannt | Logik (TDD) |
| `src/world/ground.ts` | Endlos-Boden, recentert auf Spieler | Integration |
| `src/camera/cameraRig.ts` | perspektivische Folgekamera, feste Schräg-Distanz | Integration |
| `src/tank/sockets.ts` | Socket-Namen + Kompositions-Typen | Logik |
| `src/tank/tankFactory.ts` | Panzer-Mesh-Baum auf benannten Sockets, ≥2 Varianten/Socket, Laufzeit-Tausch | Integration |
| `src/tank/tank.ts` | Panzer-Entity (HP, View) | Logik |
| `src/input/input.ts` | WASD fahren, Maus-Turm zielen, Klick feuern, M | Integration |
| `src/combat/projectilePool.ts` | gepoolte kinematische Projektile, Zustandsautomat, Invariante | Logik (TDD) |
| `src/combat/projectileView.ts` | gepoolte Babylon-Meshes ↔ Pool synchronisieren | Integration |

**Test-Disziplin:** Reine Logikmodule werden per **TDD mit Vitest** gebaut (Test zuerst, fehlschlagen sehen, minimal implementieren, bestehen sehen, committen). Babylon-Integrationsmodule sind nicht sinnvoll unit-testbar → sie werden über `npm run dev` mit konkret beobachtbarer Erwartung verifiziert (ehrlich als manuell/Preview markiert). Test-Kommando: `npx vitest run <pfad>`.

## Fundament: Verbindliche Schnittstellen-Verträge

Diese Signaturen sind über alle Tasks hinweg fix — Tasks implementieren *gegen* sie.

```ts
// src/core/rng.ts
export interface Rng { next(): number; int(maxExcl: number): number; range(min: number, max: number): number; }
export function createRng(seed: number): Rng;

// src/core/log.ts
export type LogLevel = 'debug' | 'info' | 'warn' | 'error';
export const logConfig: { enabled: boolean; minLevel: LogLevel }; // default { enabled: true, minLevel: 'debug' }
export interface Logger { debug(m: string, d?: unknown): void; info(m: string, d?: unknown): void; warn(m: string, d?: unknown): void; error(m: string, d?: unknown): void; }
export function createLogger(channel: string): Logger;

// src/core/clock.ts
export interface Clock { simSpeed: number; tick(engineDeltaMs: number): number; } // simDt[s] = (ms/1000)*clamp(simSpeed,0,1)
export function createClock(): Clock;

// src/core/events.ts
export interface GameEvents { 'tank.fired': { tankId: string }; 'projectile.spawned': { id: string }; 'projectile.consumed': { id: string }; }
export interface Bus { emit<K extends keyof GameEvents>(k: K, payload: GameEvents[K]): void; on<K extends keyof GameEvents>(k: K, fn: (p: GameEvents[K]) => void): () => void; }
export function createBus(): Bus;

// src/core/loop.ts
export function startLoop(engine: Engine, scene: Scene, clock: Clock, update: (simDt: number) => void): void;

// src/world/biomeRegistry.ts
export interface Biome { id: string; groundColor: [number, number, number]; }
export function registerBiome(b: Biome): void;
export function getBiome(id: string): Biome; // wirft Error('Unknown biome: '+id)

// src/world/ground.ts
export function createEndlessGround(scene: Scene, follow: TransformNode, biomeId: string): { update(): void };

// src/camera/cameraRig.ts
export function createCameraRig(scene: Scene, target: TransformNode): Camera;

// src/tank/sockets.ts
export type SocketName = 'chassis' | 'wheels' | 'turret' | 'weapon';
export const SOCKETS: readonly SocketName[];
export interface TankComposition { chassis: string; wheels: string; turret: string; weapon: string; }

// src/tank/tankFactory.ts
export interface TankView { root: TransformNode; turretNode: TransformNode; setVariant(socket: SocketName, variantId: string): void; }
export function createTankView(scene: Scene, comp: TankComposition): TankView;

// src/tank/tank.ts
export interface Tank { id: string; view: TankView; hp: number; maxHp: number; }
export function createTank(id: string, view: TankView, maxHp: number): Tank;

// src/input/input.ts
export function createInput(scene: Scene, tank: Tank, speed: number, onFire: () => void): { update(simDt: number): void };

// src/combat/projectilePool.ts
export type ProjectileState = 'inactive' | 'inflight' | 'consumed';
export interface Projectile { id: string; state: ProjectileState; x: number; y: number; z: number; dx: number; dz: number; speed: number; life: number; }
export interface SpawnArgs { x: number; y: number; z: number; dx: number; dz: number; speed: number; life: number; }
export interface ProjectilePool { acquire(s: SpawnArgs): Projectile | null; update(simDt: number): void; activeCount(): number; forEachActive(fn: (p: Projectile) => void): void; }
export function createProjectilePool(capacity: number): ProjectilePool;
```

---

## Tasks

### Task 1: Projekt-Konfigurationsdateien anlegen

**Files:**
- Create: `ChaosTankNew/package.json`
- Create: `ChaosTankNew/tsconfig.json`
- Create: `ChaosTankNew/vite.config.ts`
- Create: `ChaosTankNew/.gitignore`

- [ ] **Step 1: `ChaosTankNew/package.json` anlegen.** Vollständiger Inhalt:
```json
{
  "name": "chaostanknew",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@babylonjs/core": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
```

- [ ] **Step 2: `ChaosTankNew/tsconfig.json` anlegen.** Strict, ESM, bundler-Resolution, ES2022. Vollständiger Inhalt:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitReturns": true,
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,
    "types": ["vitest/globals"]
  },
  "include": ["src", "vite.config.ts", "vitest.config.ts"]
}
```

- [ ] **Step 3: `ChaosTankNew/vite.config.ts` anlegen.** Vite-Config mit eingebettetem Vitest-`test`-Feld (kein separates vitest.config.ts nötig). Vollständiger Inhalt:
```typescript
/// <reference types="vitest/config" />
import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    open: false,
  },
  test: {
    globals: true,
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
});
```

- [ ] **Step 4: `ChaosTankNew/.gitignore` anlegen.** Vollständiger Inhalt:
```
node_modules
dist
*.log
.DS_Store
```

- [ ] **Step 5: Commit.** `git add ChaosTankNew/package.json ChaosTankNew/tsconfig.json ChaosTankNew/vite.config.ts ChaosTankNew/.gitignore` + `git commit -m "chore: add project config files (package.json, tsconfig, vite/vitest, gitignore)"`

---

### Task 2: Entry-Point-Dateien (index.html + minimaler Bootstrap)

**Files:**
- Create: `ChaosTankNew/index.html`
- Create: `ChaosTankNew/src/main.ts`

- [ ] **Step 1: `ChaosTankNew/index.html` anlegen.** Canvas `#renderCanvas` über volle Höhe, lädt `src/main.ts` als Modul. Vollständiger Inhalt:
```html
<!DOCTYPE html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ChaosTankNew</title>
    <style>
      html,
      body {
        margin: 0;
        padding: 0;
        width: 100%;
        height: 100%;
        overflow: hidden;
        background: #101014;
      }
      #renderCanvas {
        width: 100%;
        height: 100%;
        display: block;
        touch-action: none;
        outline: none;
      }
    </style>
  </head>
  <body>
    <canvas id="renderCanvas"></canvas>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

- [ ] **Step 2: `ChaosTankNew/src/main.ts` anlegen (MINIMAL).** Engine auf `#renderCanvas`, leere Scene mit Kamera + Light, temporärer `engine.runRenderLoop`. HINWEIS: Dieser Render-Loop ist nur ein Platzhalter — der spätere Bootstrap-Task ersetzt ihn durch `startLoop(engine, scene, clock, update)` aus `src/core/loop.ts`. Vollständiger Inhalt:
```typescript
import {
  Engine,
  Scene,
  ArcRotateCamera,
  HemisphericLight,
  Vector3,
  Color4,
} from '@babylonjs/core';

const canvas = document.getElementById('renderCanvas') as HTMLCanvasElement;
if (!canvas) {
  throw new Error('renderCanvas not found');
}

const engine = new Engine(canvas, true, { preserveDrawingBuffer: true, stencil: true });

const scene = new Scene(engine);
// Dunkler Hintergrund (leerer Canvas-Eindruck)
scene.clearColor = new Color4(0.06, 0.06, 0.08, 1);

const camera = new ArcRotateCamera('camera', -Math.PI / 2, Math.PI / 3, 20, Vector3.Zero(), scene);
camera.attachControl(canvas, true);

const light = new HemisphericLight('light', new Vector3(0, 1, 0), scene);
light.intensity = 0.9;

// TEMPORAERER Render-Loop — wird vom Bootstrap-Task durch startLoop(...) aus src/core/loop.ts ersetzt.
engine.runRenderLoop(() => {
  scene.render();
});

window.addEventListener('resize', () => {
  engine.resize();
});
```

- [ ] **Step 3: Verifikation (manuell/Preview): `npm run dev`.** Erwartung: Vite startet ohne Fehler, Browser zeigt unter der Dev-URL einen leeren, dunklen Canvas (Hintergrund ~`#101014`/dunkelblau-grau) über die volle Fensterhöhe. Keine Konsolen-Errors. HINWEIS: Dies ist Rendering und wird manuell bzw. in der Preview verifiziert, nicht per Unit-Test.

- [ ] **Step 4: Commit.** `git add ChaosTankNew/index.html ChaosTankNew/src/main.ts` + `git commit -m "feat: add index.html and minimal Babylon bootstrap in main.ts"`

---

### Task 3: Dependencies installieren & Git initialisieren

**Files:**
- Create: `ChaosTankNew/package-lock.json` (von npm generiert)

- [ ] **Step 1: Dependencies installieren.** Im Verzeichnis `ChaosTankNew/` ausführen: `npm install`. Erwartung: legt `node_modules/` an und erzeugt `package-lock.json`. Keine Fehler (ggf. Peer-Warnings sind ok).

- [ ] **Step 2: Verifikation Test-Runner: `npx vitest run`.** Erwartung: Vitest startet und meldet sinngemäß "No test files found" bzw. läuft mit 0 Tests durch (Exit-Code 0). Das bestätigt, dass die Vitest-Config (test-Feld in `vite.config.ts`) korrekt geladen wird. HINWEIS: 0 Tests sind in diesem Scaffold-Stadium erwartet und ok.

- [ ] **Step 3: Git initialisieren.** Im Verzeichnis `ChaosTankNew/` ausführen: `git init`. Erwartung: leeres Repository angelegt; `.gitignore` sorgt dafür, dass `node_modules/` und `dist/` nicht getrackt werden.

- [ ] **Step 4: Erster Commit (gesamtes Gerüst).** `git add .` + `git commit -m "chore: initial project scaffold for ChaosTankNew"`. Erwartung: `package.json`, `tsconfig.json`, `vite.config.ts`, `.gitignore`, `index.html`, `src/main.ts`, `package-lock.json` sind committet; `node_modules/` ist ignoriert.

### Task 4: RNG (src/core/rng.ts)

**Files:**
- Create: `ChaosTankNew/src/core/rng.ts`
- Test: `ChaosTankNew/src/core/rng.test.ts`

- [ ] **Step 1: Failing-Test schreiben** — Lege `ChaosTankNew/src/core/rng.test.ts` an mit vollständigem Inhalt:
```ts
import { describe, it, expect } from 'vitest';
import { createRng } from './rng';

describe('createRng (mulberry32)', () => {
  it('liefert deterministische Sequenz für gleichen Seed', () => {
    const a = createRng(12345);
    const b = createRng(12345);
    const seqA = [a.next(), a.next(), a.next(), a.next()];
    const seqB = [b.next(), b.next(), b.next(), b.next()];
    expect(seqA).toEqual(seqB);
  });

  it('next() bleibt im Bereich [0,1)', () => {
    const r = createRng(7);
    for (let i = 0; i < 1000; i++) {
      const v = r.next();
      expect(v).toBeGreaterThanOrEqual(0);
      expect(v).toBeLessThan(1);
    }
  });

  it('unterschiedliche Seeds liefern unterschiedliche Sequenzen', () => {
    const a = createRng(1);
    const b = createRng(2);
    expect(a.next()).not.toBe(b.next());
  });

  it('int(maxExcl) liefert ganze Zahlen in [0, maxExcl)', () => {
    const r = createRng(99);
    for (let i = 0; i < 1000; i++) {
      const v = r.int(6);
      expect(Number.isInteger(v)).toBe(true);
      expect(v).toBeGreaterThanOrEqual(0);
      expect(v).toBeLessThan(6);
    }
  });

  it('range(min,max) liefert Werte in [min, max)', () => {
    const r = createRng(42);
    for (let i = 0; i < 1000; i++) {
      const v = r.range(10, 20);
      expect(v).toBeGreaterThanOrEqual(10);
      expect(v).toBeLessThan(20);
    }
  });

  it('range ist deterministisch bei gleichem Seed', () => {
    const a = createRng(555);
    const b = createRng(555);
    expect(a.range(-5, 5)).toBe(b.range(-5, 5));
  });
});
```

- [ ] **Step 2: Test ausführen — Expected: FAIL** — Run: `cd ChaosTankNew && npx vitest run src/core/rng.test.ts`. Erwartung: schlägt fehl (Modul `./rng` nicht auflösbar / `createRng` undefined).

- [ ] **Step 3: Minimale Implementierung** — Lege `ChaosTankNew/src/core/rng.ts` an mit vollständigem Inhalt:
```ts
export interface Rng {
  next(): number; // 0..1
  int(maxExcl: number): number;
  range(min: number, max: number): number;
}

export function createRng(seed: number): Rng {
  let a = seed >>> 0;
  function next(): number {
    a |= 0;
    a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  }
  return {
    next,
    int(maxExcl: number): number {
      return Math.floor(next() * maxExcl);
    },
    range(min: number, max: number): number {
      return min + next() * (max - min);
    },
  };
}
```

- [ ] **Step 4: Test ausführen — Expected: PASS** — Run: `cd ChaosTankNew && npx vitest run src/core/rng.test.ts`. Erwartung: alle Tests grün.

- [ ] **Step 5: Commit** — Run: `cd ChaosTankNew && git add src/core/rng.ts src/core/rng.test.ts && git commit -m "feat(core): deterministic mulberry32 rng with int/range"`

---

### Task 5: Logger (src/core/log.ts)

**Files:**
- Create: `ChaosTankNew/src/core/log.ts`
- Test: `ChaosTankNew/src/core/log.test.ts`

- [ ] **Step 1: Failing-Test schreiben** — Lege `ChaosTankNew/src/core/log.test.ts` an mit vollständigem Inhalt:
```ts
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { createLogger, logConfig } from './log';

describe('createLogger gate', () => {
  beforeEach(() => {
    logConfig.enabled = true;
    logConfig.minLevel = 'debug';
    vi.spyOn(console, 'debug').mockImplementation(() => {});
    vi.spyOn(console, 'info').mockImplementation(() => {});
    vi.spyOn(console, 'warn').mockImplementation(() => {});
    vi.spyOn(console, 'error').mockImplementation(() => {});
  });
  afterEach(() => {
    vi.restoreAllMocks();
  });

  it('routet auf natives console.* mit Format [level][channel] msg', () => {
    const log = createLogger('net');
    log.info('hello');
    expect(console.info).toHaveBeenCalledWith('[info][net] hello');
  });

  it('reicht optionale Daten durch', () => {
    const log = createLogger('net');
    const data = { x: 1 };
    log.warn('oops', data);
    expect(console.warn).toHaveBeenCalledWith('[warn][net] oops', data);
  });

  it('gibt nichts aus wenn enabled=false', () => {
    logConfig.enabled = false;
    const log = createLogger('net');
    log.error('nope');
    expect(console.error).not.toHaveBeenCalled();
  });

  it('unterdrückt Level unterhalb minLevel', () => {
    logConfig.minLevel = 'warn';
    const log = createLogger('net');
    log.debug('d');
    log.info('i');
    log.warn('w');
    log.error('e');
    expect(console.debug).not.toHaveBeenCalled();
    expect(console.info).not.toHaveBeenCalled();
    expect(console.warn).toHaveBeenCalledWith('[warn][net] w');
    expect(console.error).toHaveBeenCalledWith('[error][net] e');
  });

  it('Default-Config ist enabled=true, minLevel=debug', () => {
    // Erwartung gilt direkt nach Modulladen; beforeEach setzt zwar zurück,
    // prüft aber denselben Default.
    expect(logConfig.enabled).toBe(true);
    expect(logConfig.minLevel).toBe('debug');
  });
});
```

- [ ] **Step 2: Test ausführen — Expected: FAIL** — Run: `cd ChaosTankNew && npx vitest run src/core/log.test.ts`. Erwartung: schlägt fehl (Modul `./log` nicht auflösbar).

- [ ] **Step 3: Minimale Implementierung** — Lege `ChaosTankNew/src/core/log.ts` an mit vollständigem Inhalt:
```ts
export type LogLevel = 'debug' | 'info' | 'warn' | 'error';

export const logConfig: { enabled: boolean; minLevel: LogLevel } = {
  enabled: true,
  minLevel: 'debug',
};

export interface Logger {
  debug(m: string, d?: unknown): void;
  info(m: string, d?: unknown): void;
  warn(m: string, d?: unknown): void;
  error(m: string, d?: unknown): void;
}

const ORDER: Record<LogLevel, number> = { debug: 0, info: 1, warn: 2, error: 3 };

export function createLogger(channel: string): Logger {
  function gated(level: LogLevel): boolean {
    return logConfig.enabled && ORDER[level] >= ORDER[logConfig.minLevel];
  }
  function emit(level: LogLevel, m: string, d?: unknown): void {
    if (!gated(level)) return;
    const msg = `[${level}][${channel}] ${m}`;
    if (d === undefined) {
      console[level](msg);
    } else {
      console[level](msg, d);
    }
  }
  return {
    debug: (m, d) => emit('debug', m, d),
    info: (m, d) => emit('info', m, d),
    warn: (m, d) => emit('warn', m, d),
    error: (m, d) => emit('error', m, d),
  };
}
```

- [ ] **Step 4: Test ausführen — Expected: PASS** — Run: `cd ChaosTankNew && npx vitest run src/core/log.test.ts`. Erwartung: alle Tests grün.

- [ ] **Step 5: Commit** — Run: `cd ChaosTankNew && git add src/core/log.ts src/core/log.test.ts && git commit -m "feat(core): gated logger routing to native console"`

---

### Task 6: Clock (src/core/clock.ts)

**Files:**
- Create: `ChaosTankNew/src/core/clock.ts`
- Test: `ChaosTankNew/src/core/clock.test.ts`

- [ ] **Step 1: Failing-Test schreiben** — Lege `ChaosTankNew/src/core/clock.test.ts` an mit vollständigem Inhalt:
```ts
import { describe, it, expect } from 'vitest';
import { createClock } from './clock';

describe('createClock', () => {
  it('simSpeed default ist 1', () => {
    const c = createClock();
    expect(c.simSpeed).toBe(1);
  });

  it('tick liefert simDt in SEKUNDEN bei simSpeed=1', () => {
    const c = createClock();
    expect(c.tick(1000)).toBeCloseTo(1, 10);
    expect(c.tick(500)).toBeCloseTo(0.5, 10);
  });

  it('skaliert dt mit simSpeed', () => {
    const c = createClock();
    c.simSpeed = 0.5;
    expect(c.tick(1000)).toBeCloseTo(0.5, 10);
  });

  it('simSpeed=0 friert die Zeit ein', () => {
    const c = createClock();
    c.simSpeed = 0;
    expect(c.tick(1000)).toBe(0);
  });

  it('clampt simSpeed > 1 auf 1', () => {
    const c = createClock();
    c.simSpeed = 5;
    expect(c.tick(1000)).toBeCloseTo(1, 10);
  });

  it('clampt negative simSpeed auf 0', () => {
    const c = createClock();
    c.simSpeed = -3;
    expect(c.tick(1000)).toBe(0);
  });
});
```

- [ ] **Step 2: Test ausführen — Expected: FAIL** — Run: `cd ChaosTankNew && npx vitest run src/core/clock.test.ts`. Erwartung: schlägt fehl (Modul `./clock` nicht auflösbar).

- [ ] **Step 3: Minimale Implementierung** — Lege `ChaosTankNew/src/core/clock.ts` an mit vollständigem Inhalt:
```ts
export interface Clock {
  simSpeed: number; // 0..1, default 1
  tick(engineDeltaMs: number): number; // simDt in SEKUNDEN
}

function clamp01(v: number): number {
  if (v < 0) return 0;
  if (v > 1) return 1;
  return v;
}

export function createClock(): Clock {
  return {
    simSpeed: 1,
    tick(engineDeltaMs: number): number {
      return (engineDeltaMs / 1000) * clamp01(this.simSpeed);
    },
  };
}
```

- [ ] **Step 4: Test ausführen — Expected: PASS** — Run: `cd ChaosTankNew && npx vitest run src/core/clock.test.ts`. Erwartung: alle Tests grün.

- [ ] **Step 5: Commit** — Run: `cd ChaosTankNew && git add src/core/clock.ts src/core/clock.test.ts && git commit -m "feat(core): clock scaling engine dt by clamped simSpeed"`

---

### Task 7: Event-Bus (src/core/events.ts)

**Files:**
- Create: `ChaosTankNew/src/core/events.ts`
- Test: `ChaosTankNew/src/core/events.test.ts`

- [ ] **Step 1: Failing-Test schreiben** — Lege `ChaosTankNew/src/core/events.test.ts` an mit vollständigem Inhalt:
```ts
import { describe, it, expect, vi } from 'vitest';
import { createBus } from './events';

describe('createBus', () => {
  it('liefert typisiertes Payload DIREKT (kein Wrapping)', () => {
    const bus = createBus();
    const fn = vi.fn();
    bus.on('tank.fired', fn);
    bus.emit('tank.fired', { tankId: 't1' });
    expect(fn).toHaveBeenCalledWith({ tankId: 't1' });
  });

  it('ruft mehrere Listener desselben Events auf', () => {
    const bus = createBus();
    const a = vi.fn();
    const b = vi.fn();
    bus.on('projectile.spawned', a);
    bus.on('projectile.spawned', b);
    bus.emit('projectile.spawned', { id: 'p1' });
    expect(a).toHaveBeenCalledWith({ id: 'p1' });
    expect(b).toHaveBeenCalledWith({ id: 'p1' });
  });

  it('liefert nur an passenden Event-Key aus', () => {
    const bus = createBus();
    const spawned = vi.fn();
    const consumed = vi.fn();
    bus.on('projectile.spawned', spawned);
    bus.on('projectile.consumed', consumed);
    bus.emit('projectile.spawned', { id: 'p2' });
    expect(spawned).toHaveBeenCalledTimes(1);
    expect(consumed).not.toHaveBeenCalled();
  });

  it('on gibt eine unsubscribe-Funktion zurück', () => {
    const bus = createBus();
    const fn = vi.fn();
    const off = bus.on('tank.fired', fn);
    off();
    bus.emit('tank.fired', { tankId: 't2' });
    expect(fn).not.toHaveBeenCalled();
  });

  it('unsubscribe entfernt nur den eigenen Listener', () => {
    const bus = createBus();
    const a = vi.fn();
    const b = vi.fn();
    const offA = bus.on('tank.fired', a);
    bus.on('tank.fired', b);
    offA();
    bus.emit('tank.fired', { tankId: 't3' });
    expect(a).not.toHaveBeenCalled();
    expect(b).toHaveBeenCalledWith({ tankId: 't3' });
  });

  it('emit ohne Listener ist ein No-Op', () => {
    const bus = createBus();
    expect(() => bus.emit('projectile.consumed', { id: 'p3' })).not.toThrow();
  });
});
```

- [ ] **Step 2: Test ausführen — Expected: FAIL** — Run: `cd ChaosTankNew && npx vitest run src/core/events.test.ts`. Erwartung: schlägt fehl (Modul `./events` nicht auflösbar).

- [ ] **Step 3: Minimale Implementierung** — Lege `ChaosTankNew/src/core/events.ts` an mit vollständigem Inhalt:
```ts
export interface GameEvents {
  'tank.fired': { tankId: string };
  'projectile.spawned': { id: string };
  'projectile.consumed': { id: string };
}

export interface Bus {
  emit<K extends keyof GameEvents>(k: K, payload: GameEvents[K]): void;
  on<K extends keyof GameEvents>(k: K, fn: (p: GameEvents[K]) => void): () => void;
}

export function createBus(): Bus {
  const listeners: { [K in keyof GameEvents]?: Set<(p: GameEvents[K]) => void> } = {};

  return {
    emit<K extends keyof GameEvents>(k: K, payload: GameEvents[K]): void {
      const set = listeners[k];
      if (!set) return;
      for (const fn of set) {
        fn(payload);
      }
    },
    on<K extends keyof GameEvents>(k: K, fn: (p: GameEvents[K]) => void): () => void {
      let set = listeners[k];
      if (!set) {
        set = new Set();
        listeners[k] = set;
      }
      set.add(fn);
      return () => {
        set!.delete(fn);
      };
    },
  };
}
```

- [ ] **Step 4: Test ausführen — Expected: PASS** — Run: `cd ChaosTankNew && npx vitest run src/core/events.test.ts`. Erwartung: alle Tests grün.

- [ ] **Step 5: Commit** — Run: `cd ChaosTankNew && git add src/core/events.ts src/core/events.test.ts && git commit -m "feat(core): typed event bus with direct payload and unsubscribe"`

---

### Task 8: Game-Loop (src/core/loop.ts) — Babylon-Integration

**Files:**
- Create: `ChaosTankNew/src/core/loop.ts`

Hinweis: Rendering-/Engine-Integration ist NICHT unit-testbar (Babylon `runRenderLoop` braucht WebGL/Browser). Daher KEIN Vitest-Test, sondern ehrliche manuelle Verifikation, dass GENAU EIN Loop läuft und pro Frame GENAU EIN `update`-Aufruf erfolgt.

- [ ] **Step 1: Implementierung schreiben** — Lege `ChaosTankNew/src/core/loop.ts` an mit vollständigem Inhalt:
```ts
import type { Engine, Scene } from '@babylonjs/core';
import type { Clock } from './clock';

export function startLoop(
  engine: Engine,
  scene: Scene,
  clock: Clock,
  update: (simDt: number) => void,
): void {
  engine.runRenderLoop(() => {
    const simDt = clock.tick(engine.getDeltaTime());
    update(simDt);
    scene.render();
  });
}
```

- [ ] **Step 2: Wegwerf-Snippet für Frame-Zählung anlegen** — Lege `ChaosTankNew/src/core/_loopProbe.ts` an (temporär, wird in Step 4 wieder entfernt) mit vollständigem Inhalt:
```ts
// TEMPORÄRE Verifikations-Sonde für startLoop — nach Verifikation löschen.
import { Engine, Scene } from '@babylonjs/core';
import { createClock } from './clock';
import { startLoop } from './loop';

const canvas = document.createElement('canvas');
canvas.width = 320;
canvas.height = 240;
document.body.appendChild(canvas);

const engine = new Engine(canvas, true);
const scene = new Scene(engine);
const clock = createClock();

let updateCalls = 0;
let lastFrame = -1;
let frame = 0;

startLoop(engine, scene, clock, (simDt) => {
  // GENAU EIN update pro Frame: lastFrame darf nie gleich frame sein.
  if (lastFrame === frame) {
    // eslint-disable-next-line no-console
    console.error('[loopProbe] DOPPELTER update im selben Frame!');
  }
  lastFrame = frame;
  updateCalls++;
  frame++;
  if (updateCalls % 60 === 0) {
    // eslint-disable-next-line no-console
    console.log(`[loopProbe] updateCalls=${updateCalls} lastSimDt=${simDt.toFixed(4)}s`);
  }
});

window.addEventListener('resize', () => engine.resize());
```

- [ ] **Step 3: Verifikation — Run: `npm run dev`** — Starte `cd ChaosTankNew && npm run dev`, binde temporär `_loopProbe.ts` als Entry ein (oder importiere es in `src/main.ts`/`index.html`) und öffne die DevTools-Konsole. Erwartung (manuell beobachtbar):
  - Konsole zeigt fortlaufend `[loopProbe] updateCalls=60 …`, `…=120 …` etc. — die Zählung steigt monoton, ein Strom statt Sprünge → GENAU EIN Render-Loop läuft.
  - `lastSimDt` liegt bei ~0.0166s (60 FPS) und ist > 0 → `clock.tick` skaliert korrekt.
  - KEINE `[loopProbe] DOPPELTER update im selben Frame!`-Fehlerzeile erscheint → pro Frame genau ein `update`-Aufruf.
  - Es ist ehrlich anzumerken: Dieser Schritt ist eine manuelle Browser-/Preview-Verifikation, kein automatisierter Test.

- [ ] **Step 4: Sonde wieder entfernen** — Run: `cd ChaosTankNew && rm src/core/_loopProbe.ts` und entferne den temporären Import wieder aus dem Entry. Damit bleibt nur `src/core/loop.ts` als produktiver Code (die Schnittstellen-Garantie „GENAU EIN Loop“ steckt in der `startLoop`-Implementierung selbst).

- [ ] **Step 5: Commit** — Run: `cd ChaosTankNew && git add src/core/loop.ts && git commit -m "feat(core): single render loop ticking clock and update once per frame"`

---

Hinweise zu den erzeugten Dateien (alle absolut):
- `D:\Workspace_aktuall\ChaosTankNew\src\core\rng.ts` (+ `rng.test.ts`)
- `D:\Workspace_aktuall\ChaosTankNew\src\core\log.ts` (+ `log.test.ts`)
- `D:\Workspace_aktuall\ChaosTankNew\src\core\clock.ts` (+ `clock.test.ts`)
- `D:\Workspace_aktuall\ChaosTankNew\src\core\events.ts` (+ `events.test.ts`)
- `D:\Workspace_aktuall\ChaosTankNew\src\core\loop.ts`

Kontext-Befund für den Orchestrator: Im Projektordner existiert bislang NUR `ChaosTankNew/docs/` (Design-Spec unter `docs/superpowers/specs/2026-06-18-chaostank-neu-design.md`), es gibt noch KEIN `src/`, KEINE `package.json` und es ist KEIN Git-Repo. Vor der ersten Task muss daher ein Bootstrap-Step (außerhalb dieser Task-Gruppe) `npm init` + Vite/TS/Vitest/Babylon-Setup und `git init` durchführen, sonst scheitern die `npx vitest run`- und `git commit`-Steps.

_message Use this when you need to deliver a standalone status report or notification that benefits from richer formatting (headings, code blocks, links, tables, structured updates) than plain chat text provides. This renders an interstitial assistant-style message in the chat surface so a human reader can follow progress. NOTE: this is a notification channel only - the user CANNOT reply to it, so never use it to ask questions or solicit input. For your normal end-of-turn answer, just respond directly in chat instead of calling this tool.

Always write the body in the same language the surrounding conversation/system prompt uses (default to English if unsure).

Do not put the entire contents of your response in the assistant message, as it is shown to the user IN ADDITION to your normal text response. It should be used to provide standalone updates.

### Task 9: Tank-Sockets (Typen & Konstanten)

**Files:**
- Create: `ChaosTankNew/src/tank/sockets.ts`
- Test: `ChaosTankNew/src/tank/sockets.test.ts`

- [ ] **Step 1: Failing-Test schreiben** — `ChaosTankNew/src/tank/sockets.test.ts` anlegen:
```ts
import { describe, it, expect } from 'vitest';
import { SOCKETS } from './sockets';
import type { SocketName, TankComposition } from './sockets';

describe('sockets', () => {
  it('SOCKETS enthält genau die 4 Socket-Namen in Reihenfolge', () => {
    expect(SOCKETS).toEqual(['chassis', 'wheels', 'turret', 'weapon']);
  });

  it('SOCKETS hat Länge 4 und keine Duplikate', () => {
    expect(SOCKETS).toHaveLength(4);
    expect(new Set(SOCKETS).size).toBe(4);
  });

  it('jeder SOCKET-Eintrag ist ein gültiger SocketName', () => {
    const allowed: SocketName[] = ['chassis', 'wheels', 'turret', 'weapon'];
    for (const s of SOCKETS) {
      expect(allowed).toContain(s);
    }
  });

  it('TankComposition akzeptiert je Socket eine Varianten-ID', () => {
    const comp: TankComposition = {
      chassis: 'c_box',
      wheels: 'w_round',
      turret: 't_small',
      weapon: 'g_short',
    };
    expect(comp.chassis).toBe('c_box');
    expect(comp.weapon).toBe('g_short');
  });
});
```

- [ ] **Step 2: Test ausführen (Expected: FAIL)** — `npx vitest run src/tank/sockets.test.ts` ausführen. Erwartung: schlägt fehl, weil `src/tank/sockets.ts` noch nicht existiert (Modul nicht auflösbar).

- [ ] **Step 3: Minimale Implementierung** — `ChaosTankNew/src/tank/sockets.ts` anlegen:
```ts
export type SocketName = 'chassis' | 'wheels' | 'turret' | 'weapon';

export const SOCKETS: readonly SocketName[] = ['chassis', 'wheels', 'turret', 'weapon'] as const;

export interface TankComposition {
  chassis: string;
  wheels: string;
  turret: string;
  weapon: string;
}
```

- [ ] **Step 4: Test ausführen (Expected: PASS)** — `npx vitest run src/tank/sockets.test.ts` ausführen. Erwartung: alle 4 Tests grün.

- [ ] **Step 5: Commit** — `git add src/tank/sockets.ts src/tank/sockets.test.ts && git commit -m "feat(tank): add socket types and SOCKETS constant"`

---

### Task 10: TankFactory (TransformNode-Baum mit Varianten)

**Files:**
- Create: `ChaosTankNew/src/tank/tankFactory.ts`

- [ ] **Step 1: TankFactory implementieren** — `ChaosTankNew/src/tank/tankFactory.ts` anlegen (Babylon-Integration, vollständiger Code; baut root mit 4 Socket-TransformNodes, je >=2 Varianten als gefärbte Primitives, setVariant disposed altes Kind-Mesh):
```ts
import {
  MeshBuilder,
  StandardMaterial,
  Color3,
  TransformNode,
  Vector3,
} from '@babylonjs/core';
import type { Scene, Mesh } from '@babylonjs/core';
import type { SocketName, TankComposition } from './sockets';
import { SOCKETS } from './sockets';

export interface TankView {
  root: TransformNode;
  turretNode: TransformNode;
  setVariant(socket: SocketName, variantId: string): void;
}

interface SocketRuntime {
  node: TransformNode;
  mesh: Mesh | null;
  variantId: string;
}

function makeMaterial(scene: Scene, name: string, rgb: [number, number, number]): StandardMaterial {
  const mat = new StandardMaterial(name, scene);
  mat.diffuseColor = new Color3(rgb[0], rgb[1], rgb[2]);
  return mat;
}

function buildVariantMesh(scene: Scene, socket: SocketName, variantId: string): Mesh {
  let mesh: Mesh;
  switch (socket) {
    case 'chassis':
      if (variantId === 'c_wide') {
        mesh = MeshBuilder.CreateBox('chassis_mesh', { width: 2.4, height: 0.6, depth: 3.0 }, scene);
        mesh.material = makeMaterial(scene, 'chassis_mat', [0.35, 0.30, 0.20]);
      } else {
        // c_box (default)
        mesh = MeshBuilder.CreateBox('chassis_mesh', { width: 1.6, height: 0.8, depth: 2.6 }, scene);
        mesh.material = makeMaterial(scene, 'chassis_mat', [0.45, 0.40, 0.28]);
      }
      mesh.position = new Vector3(0, 0.4, 0);
      break;
    case 'wheels':
      if (variantId === 'w_tread') {
        mesh = MeshBuilder.CreateBox('wheels_mesh', { width: 1.9, height: 0.5, depth: 2.8 }, scene);
        mesh.material = makeMaterial(scene, 'wheels_mat', [0.12, 0.12, 0.12]);
      } else {
        // w_round (default)
        mesh = MeshBuilder.CreateCylinder('wheels_mesh', { diameter: 0.8, height: 2.0 }, scene);
        mesh.rotation = new Vector3(0, 0, Math.PI / 2);
        mesh.material = makeMaterial(scene, 'wheels_mat', [0.18, 0.18, 0.18]);
      }
      mesh.position = new Vector3(0, 0.2, 0);
      break;
    case 'turret':
      if (variantId === 't_big') {
        mesh = MeshBuilder.CreateBox('turret_mesh', { width: 1.4, height: 0.7, depth: 1.4 }, scene);
        mesh.material = makeMaterial(scene, 'turret_mat', [0.55, 0.50, 0.35]);
      } else {
        // t_small (default)
        mesh = MeshBuilder.CreateBox('turret_mesh', { width: 1.0, height: 0.5, depth: 1.0 }, scene);
        mesh.material = makeMaterial(scene, 'turret_mat', [0.60, 0.55, 0.40]);
      }
      mesh.position = new Vector3(0, 0.25, 0);
      break;
    case 'weapon':
      if (variantId === 'g_long') {
        mesh = MeshBuilder.CreateCylinder('weapon_mesh', { diameter: 0.22, height: 2.4 }, scene);
        mesh.material = makeMaterial(scene, 'weapon_mat', [0.30, 0.30, 0.33]);
        mesh.position = new Vector3(0, 0, 1.4);
      } else {
        // g_short (default)
        mesh = MeshBuilder.CreateCylinder('weapon_mesh', { diameter: 0.28, height: 1.4 }, scene);
        mesh.material = makeMaterial(scene, 'weapon_mat', [0.38, 0.38, 0.40]);
        mesh.position = new Vector3(0, 0, 0.9);
      }
      mesh.rotation = new Vector3(Math.PI / 2, 0, 0);
      break;
    default: {
      const _exhaustive: never = socket;
      throw new Error('Unknown socket: ' + String(_exhaustive));
    }
  }
  return mesh;
}

export function createTankView(scene: Scene, comp: TankComposition): TankView {
  const root = new TransformNode('tank_root', scene);

  const runtimes: Record<SocketName, SocketRuntime> = {} as Record<SocketName, SocketRuntime>;

  for (const socket of SOCKETS) {
    const node = new TransformNode('socket_' + socket, scene);
    node.parent = root;
    runtimes[socket] = { node, mesh: null, variantId: comp[socket] };
  }

  // socket_turret sitzt höher; weapon hängt unter turret
  runtimes.turret.node.position = new Vector3(0, 0.9, 0);
  runtimes.weapon.node.parent = runtimes.turret.node;
  runtimes.weapon.node.position = new Vector3(0, 0.25, 0);

  function applyVariant(socket: SocketName, variantId: string): void {
    const rt = runtimes[socket];
    if (rt.mesh) {
      rt.mesh.dispose();
      rt.mesh = null;
    }
    const mesh = buildVariantMesh(scene, socket, variantId);
    mesh.parent = rt.node;
    rt.mesh = mesh;
    rt.variantId = variantId;
  }

  for (const socket of SOCKETS) {
    applyVariant(socket, comp[socket]);
  }

  return {
    root,
    turretNode: runtimes.turret.node,
    setVariant(socket: SocketName, variantId: string): void {
      applyVariant(socket, variantId);
    },
  };
}
```

- [ ] **Step 2: Verifikation (manuell/Preview)** — `npm run dev` starten und im Browser öffnen. Erwartung (manuell sichtbar, da Rendering nicht unit-testbar): Ein Panzer aus mehreren Teilen ist sichtbar — flacher Chassis-Block, Räder/Tread darunter, ein Turm-Block oben mit einem Rohr nach vorne. Beim Aufruf von `view.setVariant('chassis','c_wide')` bzw. `view.setVariant('weapon','g_long')` (testweise z.B. in `main.ts` oder Konsole) ändert sich sichtbar die Form (breiteres Chassis / längeres Rohr), und es bleibt genau EIN Mesh pro Socket (kein Doppel-Mesh, altes wird disposed). HINWEIS: Diese Prüfung ist visuell/manuell, kein automatischer Test.

- [ ] **Step 3: Commit** — `git add src/tank/tankFactory.ts && git commit -m "feat(tank): add tankFactory building socket node tree with swappable variants"`

---

### Task 11: Tank (Datenmodell)

**Files:**
- Create: `ChaosTankNew/src/tank/tank.ts`
- Test: `ChaosTankNew/src/tank/tank.test.ts`

- [ ] **Step 1: Failing-Test schreiben** — `ChaosTankNew/src/tank/tank.test.ts` anlegen (TankView via Minimal-Stub gemockt, da rein logische Feld-Prüfung):
```ts
import { describe, it, expect } from 'vitest';
import { createTank } from './tank';
import type { TankView } from './tankFactory';

function makeStubView(): TankView {
  // Minimal-Stub: createTank berührt die View-Felder nicht, nur Referenz halten.
  return {
    root: {} as TankView['root'],
    turretNode: {} as TankView['turretNode'],
    setVariant: () => {},
  };
}

describe('createTank', () => {
  it('setzt id, view, hp=maxHp und maxHp korrekt', () => {
    const view = makeStubView();
    const tank = createTank('t1', view, 100);
    expect(tank.id).toBe('t1');
    expect(tank.view).toBe(view);
    expect(tank.maxHp).toBe(100);
    expect(tank.hp).toBe(100);
  });

  it('startet mit voller HP (hp === maxHp)', () => {
    const tank = createTank('t2', makeStubView(), 42);
    expect(tank.hp).toBe(tank.maxHp);
    expect(tank.hp).toBe(42);
  });

  it('verschiedene IDs bleiben unabhängig erhalten', () => {
    const a = createTank('alpha', makeStubView(), 10);
    const b = createTank('beta', makeStubView(), 20);
    expect(a.id).toBe('alpha');
    expect(b.id).toBe('beta');
    expect(a.maxHp).toBe(10);
    expect(b.maxHp).toBe(20);
  });
});
```

- [ ] **Step 2: Test ausführen (Expected: FAIL)** — `npx vitest run src/tank/tank.test.ts` ausführen. Erwartung: schlägt fehl, weil `src/tank/tank.ts` noch nicht existiert.

- [ ] **Step 3: Minimale Implementierung** — `ChaosTankNew/src/tank/tank.ts` anlegen:
```ts
import type { TankView } from './tankFactory';

export interface Tank {
  id: string;
  view: TankView;
  hp: number;
  maxHp: number;
}

export function createTank(id: string, view: TankView, maxHp: number): Tank {
  return {
    id,
    view,
    hp: maxHp,
    maxHp,
  };
}
```

- [ ] **Step 4: Test ausführen (Expected: PASS)** — `npx vitest run src/tank/tank.test.ts` ausführen. Erwartung: alle 3 Tests grün.

- [ ] **Step 5: Commit** — `git add src/tank/tank.ts src/tank/tank.test.ts && git commit -m "feat(tank): add Tank model and createTank factory"`

---

### Task 12: Input (WASD / Maus-Turm / Feuer / Map-Toggle)

**Files:**
- Create: `ChaosTankNew/src/input/input.ts`

- [ ] **Step 1: Input implementieren** — `ChaosTankNew/src/input/input.ts` anlegen (Babylon-Integration, vollständiger Code; WASD bewegt `tank.view.root`, Maus-Pointer auf Boden via `scene.pick` dreht `tank.view.turretNode` per `Math.atan2`, Linksklick -> `onFire()`, Taste 'm' -> Logger 'map toggle'):
```ts
import { Vector3, PointerEventTypes } from '@babylonjs/core';
import type { Scene, PickingInfo } from '@babylonjs/core';
import type { Tank } from '../tank/tank';
import { createLogger } from '../core/log';

export function createInput(
  scene: Scene,
  tank: Tank,
  speed: number,
  onFire: () => void,
): { update(simDt: number): void } {
  const log = createLogger('input');

  const keys: Record<string, boolean> = Object.create(null);
  let pointerGroundTarget: Vector3 | null = null;

  function isGroundPick(info: PickingInfo | null): boolean {
    return !!info && info.hit && !!info.pickedPoint;
  }

  const onKeyDown = (ev: KeyboardEvent): void => {
    const k = ev.key.toLowerCase();
    keys[k] = true;
    if (k === 'm') {
      log.info('map toggle');
    }
  };
  const onKeyUp = (ev: KeyboardEvent): void => {
    keys[ev.key.toLowerCase()] = false;
  };

  window.addEventListener('keydown', onKeyDown);
  window.addEventListener('keyup', onKeyUp);

  scene.onPointerObservable.add((pointerInfo) => {
    if (pointerInfo.type === PointerEventTypes.POINTERMOVE) {
      const pick = scene.pick(scene.pointerX, scene.pointerY);
      if (isGroundPick(pick) && pick.pickedPoint) {
        pointerGroundTarget = pick.pickedPoint.clone();
      }
    } else if (pointerInfo.type === PointerEventTypes.POINTERDOWN) {
      // button 0 = Linksklick
      if (pointerInfo.event.button === 0) {
        onFire();
      }
    }
  });

  function update(simDt: number): void {
    const root = tank.view.root;

    // WASD: Bewegung in X/Z-Ebene, distanz = speed * simDt
    let mx = 0;
    let mz = 0;
    if (keys['w']) mz += 1;
    if (keys['s']) mz -= 1;
    if (keys['a']) mx -= 1;
    if (keys['d']) mx += 1;

    if (mx !== 0 || mz !== 0) {
      const len = Math.hypot(mx, mz);
      const step = speed * simDt;
      root.position.addInPlace(new Vector3((mx / len) * step, 0, (mz / len) * step));
    }

    // Maus: Turm zum Boden-Pointer drehen (Y-Yaw via atan2)
    if (pointerGroundTarget) {
      const turret = tank.view.turretNode;
      const worldRoot = root.position;
      const dx = pointerGroundTarget.x - worldRoot.x;
      const dz = pointerGroundTarget.z - worldRoot.z;
      if (dx !== 0 || dz !== 0) {
        turret.rotation.y = Math.atan2(dx, dz);
      }
    }
  }

  return { update };
}
```

- [ ] **Step 2: Verifikation (manuell/Preview)** — `npm run dev` starten und im Browser öffnen. Erwartung (manuell beobachtbar, kein Unit-Test möglich): W/A/S/D bewegt den Panzer flüssig über den Boden (Geschwindigkeit framerate-unabhängig dank `speed*simDt`); Mausbewegung dreht den Turm so, dass das Rohr zum Boden-Punkt unter dem Cursor zeigt; Linksklick löst `onFire()` aus (z.B. ein Projektil/Log sichtbar, je nach Verdrahtung in `main.ts`); Taste 'm' erzeugt in der Browser-Konsole die Logausgabe `[info][input] map toggle`. HINWEIS: visuelle/manuelle Prüfung, da Eingabe & Rendering nicht unit-testbar.

- [ ] **Step 3: Commit** — `git add src/input/input.ts && git commit -m "feat(input): add WASD movement, mouse turret aim, fire and map-toggle input"`

### Task 13: projectilePool (logisch, TDD)

**Files:**
- Create: `ChaosTankNew/src/combat/projectilePool.ts`
- Test: `ChaosTankNew/src/combat/projectilePool.test.ts`

- [ ] **Step 1: Test schreiben — `acquire` setzt state und ALLE Felder zurück.** Lege `ChaosTankNew/src/combat/projectilePool.test.ts` an:
```ts
import { describe, it, expect } from 'vitest';
import { createProjectilePool } from './projectilePool';

describe('projectilePool.acquire', () => {
  it('liefert ein Projektil mit state "inflight" und allen gesetzten Feldern', () => {
    const pool = createProjectilePool(2);
    const p = pool.acquire({ x: 1, y: 2, z: 3, dx: 0, dz: 1, speed: 10, life: 5 });
    expect(p).not.toBeNull();
    expect(p!.state).toBe('inflight');
    expect(p!.x).toBe(1);
    expect(p!.y).toBe(2);
    expect(p!.z).toBe(3);
    expect(p!.dx).toBe(0);
    expect(p!.dz).toBe(1);
    expect(p!.speed).toBe(10);
    expect(p!.life).toBe(5);
    expect(p!.id).toBeTypeOf('string');
  });

  it('liefert null wenn der Pool voll ist', () => {
    const pool = createProjectilePool(1);
    pool.acquire({ x: 0, y: 0, z: 0, dx: 1, dz: 0, speed: 1, life: 1 });
    const second = pool.acquire({ x: 0, y: 0, z: 0, dx: 1, dz: 0, speed: 1, life: 1 });
    expect(second).toBeNull();
  });

  it('recyceltes Objekt trägt KEINE alten Werte (kein Leak)', () => {
    const pool = createProjectilePool(1);
    const first = pool.acquire({ x: 99, y: 99, z: 99, dx: -7, dz: -7, speed: 999, life: 0.0001 });
    expect(first).not.toBeNull();
    pool.update(1); // life<=0 -> consumed -> inactive
    expect(pool.activeCount()).toBe(0);
    const reused = pool.acquire({ x: 5, y: 6, z: 7, dx: 1, dz: 0, speed: 2, life: 3 });
    expect(reused).not.toBeNull();
    expect(reused!.x).toBe(5);
    expect(reused!.y).toBe(6);
    expect(reused!.z).toBe(7);
    expect(reused!.dx).toBe(1);
    expect(reused!.dz).toBe(0);
    expect(reused!.speed).toBe(2);
    expect(reused!.life).toBe(3);
    expect(reused!.state).toBe('inflight');
  });
});
```

- [ ] **Step 2: Test ausführen → Expected: FAIL.** Run: `npx vitest run src/combat/projectilePool.test.ts` (im Verzeichnis `ChaosTankNew/`). Erwartung: schlägt fehl, weil `projectilePool.ts` noch nicht existiert (Modul nicht auflösbar).

- [ ] **Step 3: Test schreiben — `update` bewegt kinematisch und verbraucht bei life<=0.** Ergänze in `ChaosTankNew/src/combat/projectilePool.test.ts`:
```ts
describe('projectilePool.update', () => {
  it('bewegt inflight mit x+=dx*speed*simDt und z+=dz*speed*simDt', () => {
    const pool = createProjectilePool(1);
    const p = pool.acquire({ x: 0, y: 0, z: 0, dx: 1, dz: 0.5, speed: 4, life: 100 });
    pool.update(0.5);
    expect(p!.x).toBeCloseTo(0 + 1 * 4 * 0.5, 6);  // 2
    expect(p!.z).toBeCloseTo(0 + 0.5 * 4 * 0.5, 6); // 1
    expect(p!.life).toBeCloseTo(99.5, 6);
    expect(p!.state).toBe('inflight');
  });

  it('setzt life<=0 -> consumed -> recycelt zu inactive (nicht mehr aktiv)', () => {
    const pool = createProjectilePool(1);
    pool.acquire({ x: 0, y: 0, z: 0, dx: 1, dz: 0, speed: 1, life: 1 });
    expect(pool.activeCount()).toBe(1);
    pool.update(1); // life 1 -> 0 -> consumed -> inactive
    expect(pool.activeCount()).toBe(0);
  });
});
```

- [ ] **Step 4: Test schreiben — INVARIANTE activeCount() == Zahl der inflight (kein Phantom).** Ergänze in `ChaosTankNew/src/combat/projectilePool.test.ts`:
```ts
describe('projectilePool Invariante', () => {
  it('activeCount() entspricht nach beliebiger Sequenz der Zahl der inflight-Objekte', () => {
    const pool = createProjectilePool(4);
    pool.acquire({ x: 0, y: 0, z: 0, dx: 1, dz: 0, speed: 1, life: 10 });
    pool.acquire({ x: 0, y: 0, z: 0, dx: 1, dz: 0, speed: 1, life: 2 });
    pool.acquire({ x: 0, y: 0, z: 0, dx: 1, dz: 0, speed: 1, life: 0.5 });
    pool.update(1); // ein Projektil (life 0.5) verfällt

    let counted = 0;
    pool.forEachActive((p) => {
      expect(p.state).toBe('inflight');
      counted++;
    });
    expect(counted).toBe(pool.activeCount());
    expect(pool.activeCount()).toBe(2);

    // wieder auffüllen und prüfen, dass recycelte Slots korrekt zählen
    pool.acquire({ x: 0, y: 0, z: 0, dx: 1, dz: 0, speed: 1, life: 5 });
    pool.acquire({ x: 0, y: 0, z: 0, dx: 1, dz: 0, speed: 1, life: 5 });
    let counted2 = 0;
    pool.forEachActive(() => { counted2++; });
    expect(counted2).toBe(pool.activeCount());
    expect(pool.activeCount()).toBe(4);

    // forEachActive iteriert NUR über inflight (kein consumed/inactive)
    pool.update(100); // alles verfällt
    let counted3 = 0;
    pool.forEachActive(() => { counted3++; });
    expect(counted3).toBe(0);
    expect(pool.activeCount()).toBe(0);
  });
});
```

- [ ] **Step 5: Test ausführen → Expected: FAIL.** Run: `npx vitest run src/combat/projectilePool.test.ts`. Erwartung: alle Tests scheitern (Modul fehlt weiterhin).

- [ ] **Step 6: Minimale Implementierung `projectilePool.ts`.** Lege `ChaosTankNew/src/combat/projectilePool.ts` an:
```ts
export type ProjectileState = 'inactive' | 'inflight' | 'consumed';

export interface Projectile {
  id: string;
  state: ProjectileState;
  x: number;
  y: number;
  z: number;
  dx: number;
  dz: number;
  speed: number;
  life: number;
}

export interface SpawnArgs {
  x: number;
  y: number;
  z: number;
  dx: number;
  dz: number;
  speed: number;
  life: number;
}

export interface ProjectilePool {
  acquire(s: SpawnArgs): Projectile | null;
  update(simDt: number): void;
  activeCount(): number;
  forEachActive(fn: (p: Projectile) => void): void;
}

export function createProjectilePool(capacity: number): ProjectilePool {
  const items: Projectile[] = [];
  for (let i = 0; i < capacity; i++) {
    items.push({
      id: 'proj_' + i,
      state: 'inactive',
      x: 0,
      y: 0,
      z: 0,
      dx: 0,
      dz: 0,
      speed: 0,
      life: 0,
    });
  }

  function acquire(s: SpawnArgs): Projectile | null {
    for (let i = 0; i < items.length; i++) {
      const p = items[i]!;
      if (p.state === 'inactive') {
        p.x = s.x;
        p.y = s.y;
        p.z = s.z;
        p.dx = s.dx;
        p.dz = s.dz;
        p.speed = s.speed;
        p.life = s.life;
        p.state = 'inflight';
        return p;
      }
    }
    return null;
  }

  function update(simDt: number): void {
    for (let i = 0; i < items.length; i++) {
      const p = items[i]!;
      if (p.state !== 'inflight') continue;
      p.x += p.dx * p.speed * simDt;
      p.z += p.dz * p.speed * simDt;
      p.life -= simDt;
      if (p.life <= 0) {
        p.state = 'consumed';
        p.state = 'inactive';
      }
    }
  }

  function activeCount(): number {
    let n = 0;
    for (let i = 0; i < items.length; i++) {
      if (items[i]!.state === 'inflight') n++;
    }
    return n;
  }

  function forEachActive(fn: (p: Projectile) => void): void {
    for (let i = 0; i < items.length; i++) {
      const p = items[i]!;
      if (p.state === 'inflight') fn(p);
    }
  }

  return { acquire, update, activeCount, forEachActive };
}
```

- [ ] **Step 7: Test ausführen → Expected: PASS.** Run: `npx vitest run src/combat/projectilePool.test.ts`. Erwartung: alle Tests grün (acquire-Reset, kinematik+Verfall, Invariante).

- [ ] **Step 8: Commit.** Run: `git add src/combat/projectilePool.ts src/combat/projectilePool.test.ts && git commit -m "feat(combat): add logical projectile pool with TDD coverage"`.

---

### Task 14: projectileView + Feuern (Babylon-Integration)

**Files:**
- Create: `ChaosTankNew/src/combat/projectileView.ts`
- Verify: manuell via `npm run dev` (Rendering nicht unit-testbar)

- [ ] **Step 1: `projectileView.ts` anlegen — gepoolte Babylon-Kugeln synchron zum Pool.** Lege `ChaosTankNew/src/combat/projectileView.ts` an. Diese View hält eine feste Kugel pro Pool-Slot (Mesh-Größe == Pool-Größe), zeigt nur die inflight-Kugeln und liefert ein `onFire()`, das aus `tank.view.turretNode` Position + Blickrichtung `acquire()` ruft:
```ts
import { MeshBuilder, StandardMaterial, Color3 } from '@babylonjs/core';
import type { Scene, Mesh } from '@babylonjs/core';
import type { ProjectilePool, Projectile } from './projectilePool';

export interface ProjectileView {
  /** Synchronisiert sichtbare Meshes mit den aktiven (inflight) Projektilen des Pools. */
  sync(): void;
  /** Anzahl aktuell SICHTBARER (enabled) Projektil-Meshes — fuer §21.5 Log-Abgleich. */
  visibleCount(): number;
  dispose(): void;
}

/**
 * Erzeugt capacity vorab-allokierte Kugel-Meshes (Pooling, kein Spawn/Dispose im Flug).
 * sync() bindet jedes inflight-Projektil an genau ein Mesh und versteckt den Rest.
 */
export function createProjectileView(
  scene: Scene,
  pool: ProjectilePool,
  capacity: number
): ProjectileView {
  const mat = new StandardMaterial('projectileMat', scene);
  mat.diffuseColor = new Color3(1, 0.85, 0.2);
  mat.emissiveColor = new Color3(0.8, 0.6, 0.05);

  const meshes: Mesh[] = [];
  for (let i = 0; i < capacity; i++) {
    const m = MeshBuilder.CreateSphere(`projectile_${i}`, { diameter: 0.4, segments: 6 }, scene);
    m.material = mat;
    m.isPickable = false;
    m.setEnabled(false);
    meshes.push(m);
  }

  let visible = 0;

  function sync(): void {
    let i = 0;
    pool.forEachActive((p: Projectile) => {
      const m = meshes[i];
      if (m) {
        m.position.set(p.x, p.y, p.z);
        m.setEnabled(true);
      }
      i++;
    });
    for (let j = i; j < meshes.length; j++) {
      meshes[j].setEnabled(false);
    }
    visible = i;
  }

  function visibleCount(): number {
    return visible;
  }

  function dispose(): void {
    for (const m of meshes) m.dispose();
    mat.dispose();
  }

  return { sync, visibleCount, dispose };
}
```

- [ ] **Step 2: TypeScript-Build prüfen → Expected: PASS.** Run: `npx tsc -b` (im Verzeichnis `ChaosTankNew/`). Erwartung: keine Typfehler in `projectileView.ts` (strict). Dies prüft die Babylon-Importe/Signaturen, ersetzt aber keinen Rendering-Test.

- [ ] **Step 3: Commit.** Run: `git add src/combat/projectileView.ts && git commit -m "feat(combat): add pooled projectile mesh bridge (projectileView)"`.

> `projectileView` ist rein die **Mesh-Brücke** (sync/visibleCount/dispose). Die eigentliche **Verdrahtung in `main.ts`** (Pool erzeugen, View bauen, Feuern aus der Turm-Richtung, einziger Loop) und die **visuelle §21.5-Verifikation** (gelbe Kugel an der Mündung, `active == visible` im Log, kein Phantomschuss, Pool-Voll → „shot dropped") erfolgen in **Task 15**. Die testbare Pool-Invariante (`activeCount()` == Zahl der inflight-Objekte) ist bereits in Task 13 per Vitest abgedeckt.

### Task 15: Finaler src/main.ts (Bootstrap mit Start-Screen, genau ein startLoop)

**Files:**
- Create: `ChaosTankNew/src/main.ts`
- Use (aus Task 14): `ChaosTankNew/src/combat/projectileView.ts`

> Hinweis: Reines Babylon-Integrationsmodul (DOM, Engine, Scene, Render) — **kein Unit-Test** (Rendering ist nicht unit-testbar). Verifikation erfolgt manuell via `npm run dev` / Preview mit konkret beobachtbaren Erwartungen. Voraussetzung: alle Schnittstellen-Module (`core/*`, `world/*`, `camera/*`, `tank/*`, `input/*`, `combat/projectilePool`) existieren bereits aus den vorherigen Task-Gruppen.

- [ ] **Step 1: `projectileView.ts` NICHT neu anlegen** — die Datei wird in **Task 14** erstellt (kanonische API `createProjectileView(scene, pool, capacity)` → `sync()` / `visibleCount()` / `dispose()`). `main.ts` unten importiert sie nur. Falls Task 14 noch nicht ausgeführt wurde, zuerst Task 14 erledigen.

- [ ] **Step 2: `src/main.ts` anlegen — Imports, Konstanten, Start-Screen-DOM** (oberer Teil der Datei). Vollständiger Code:
```ts
import { Engine, Scene, HemisphericLight, Vector3, Color4 } from '@babylonjs/core';
import { createClock } from './core/clock';
import { createBus } from './core/events';
import { createLogger, logConfig } from './core/log';
import { createRng } from './core/rng';
import { registerBiome, getBiome } from './world/biomeRegistry';
import { createEndlessGround } from './world/ground';
import { createCameraRig } from './camera/cameraRig';
import { createTankView } from './tank/tankFactory';
import { createTank } from './tank/tank';
import { createInput } from './input/input';
import { createProjectilePool } from './combat/projectilePool';
import { createProjectileView } from './combat/projectileView';
import type { TankComposition } from './tank/sockets';

const BIOME_ID = 'steppe';
const TANK_SPEED = 8;          // Welt-Einheiten/s
const PROJECTILE_CAPACITY = 64;
const PROJECTILE_SPEED = 30;
const PROJECTILE_LIFE = 3;     // Sekunden
const SEED = 1337;

const log = createLogger('main');

function mountStartScreen(onStart: () => void): void {
  const overlay = document.createElement('div');
  overlay.id = 'start-screen';
  overlay.style.cssText =
    'position:fixed;inset:0;display:flex;align-items:center;justify-content:center;' +
    'background:#0b0d10;z-index:10;font-family:system-ui,sans-serif;';

  const btn = document.createElement('button');
  btn.textContent = 'Start';
  btn.style.cssText =
    'padding:16px 48px;font-size:20px;cursor:pointer;border:2px solid #d8b04a;' +
    'background:#1a1d22;color:#f0e6cc;border-radius:8px;';
  btn.addEventListener('click', () => {
    overlay.remove();
    onStart();
  });

  overlay.appendChild(btn);
  document.body.appendChild(overlay);
}
```

- [ ] **Step 3: `src/main.ts` ergänzen — `boot()` (Engine/Scene/Licht/Welt/Panzer/Pool/Loop) + Aufruf** (unterer Teil, an Step 2 anhängen). GENAU EIN `startLoop`. Vollständiger Code:
```ts
function getCanvas(): HTMLCanvasElement {
  let canvas = document.getElementById('renderCanvas') as HTMLCanvasElement | null;
  if (!canvas) {
    canvas = document.createElement('canvas');
    canvas.id = 'renderCanvas';
    canvas.style.cssText = 'position:fixed;inset:0;width:100%;height:100%;display:block;';
    document.body.appendChild(canvas);
  }
  return canvas;
}

function boot(): void {
  log.info('boot start', { seed: SEED, biome: BIOME_ID });

  const canvas = getCanvas();
  const engine = new Engine(canvas, true);
  const scene = new Scene(engine);
  scene.clearColor = new Color4(0.05, 0.06, 0.07, 1);

  // Kerndienste
  const clock = createClock();
  const bus = createBus();
  const rng = createRng(SEED);

  // Licht
  const light = new HemisphericLight('sun', new Vector3(0.3, 1, 0.2), scene);
  light.intensity = 0.95;

  // Biom 'steppe' ist beim Modulladen registriert; defensiv sicherstellen + Negativ-faehig.
  registerBiome({ id: BIOME_ID, groundColor: [0.36, 0.4, 0.24] });
  const biome = getBiome(BIOME_ID);
  log.debug('biome resolved', { id: biome.id, groundColor: biome.groundColor });

  // Panzer aus modularen Teilen komponieren
  const comp: TankComposition = {
    chassis: 'c_box',
    wheels: 'w_round',
    turret: 't_small',
    weapon: 'g_short',
  };
  const view = createTankView(scene, comp);
  const tank = createTank('player', view, 100);
  log.info('tank built', { id: tank.id, hp: tank.hp, comp });

  // Endlos-Boden folgt dem Panzer-Root
  const ground = createEndlessGround(scene, tank.view.root, BIOME_ID);

  // Kamera auf den Panzer-Root
  createCameraRig(scene, tank.view.root);

  // Projektil-Pool (rein logisch) + sichtbare Mesh-Bruecke
  const pool = createProjectilePool(PROJECTILE_CAPACITY);
  const projectileView = createProjectileView(scene, pool, PROJECTILE_CAPACITY);

  // Feuern: Richtung aus der Turm-Weltmatrix ableiten (vorwaerts = +Z lokal)
  function fire(): void {
    const root = tank.view.root;
    const turret = tank.view.turretNode;
    turret.computeWorldMatrix(true);
    const fwd = turret.forward; // Babylon: normalisierte Welt-Vorwaertsrichtung
    const origin = root.getAbsolutePosition();
    const p = pool.acquire({
      x: origin.x,
      y: 0.5,
      z: origin.z,
      dx: fwd.x,
      dz: fwd.z,
      speed: PROJECTILE_SPEED,
      life: PROJECTILE_LIFE,
    });
    if (p) {
      bus.emit('tank.fired', { tankId: tank.id });
      bus.emit('projectile.spawned', { id: p.id });
      log.debug('fired', { tankId: tank.id, projectile: p.id, dx: fwd.x, dz: fwd.z });
    } else {
      log.warn('pool full, shot dropped', { capacity: PROJECTILE_CAPACITY });
    }
  }

  const input = createInput(scene, tank, TANK_SPEED, fire);

  // §21.5-Sichtbarkeitszaehler periodisch loggen (aktiv == sichtbar)
  let frame = 0;

  // GENAU EIN Loop.
  // startLoop ruft engine.runRenderLoop; update bekommt simDt in Sekunden.
  // Reihenfolge: Steuerung -> Pool-Logik -> Boden-Recenter -> Mesh-Sync.
  import('./core/loop').then(({ startLoop }) => {
    startLoop(engine, scene, clock, (simDt) => {
      input.update(simDt);
      pool.update(simDt);
      ground.update();
      projectileView.sync();

      frame++;
      if (frame % 60 === 0) {
        const active = pool.activeCount();
        const visible = projectileView.visibleCount();
        log.debug('pool state', { active, visible, match: active === visible });
      }
    });
    log.info('loop running');
  });

  // RNG bleibt fuer spaetere deterministische Effekte gebunden (Slice 1: nur Beleg).
  void rng;

  window.addEventListener('resize', () => engine.resize());
}

// Logging scharf schalten.
logConfig.enabled = true;
logConfig.minLevel = 'debug';

mountStartScreen(boot);
```

- [ ] **Step 4: Verifikation — `cd ChaosTankNew && npm run dev`** (manuell/Preview, da Babylon-Rendering nicht unit-testbar). Erwartung, beobachtbar:
  - Beim Laden erscheint ein zentrierter **„Start"-Button** über dunklem Hintergrund; die Babylon-Szene rendert NICHT vor dem Klick (Konsole zeigt nur `[info][main] boot start` **nach** Klick, nicht davor).
  - Nach Klick verschwindet der Button, ein steppenfarbener Endlos-Boden + Panzer (Chassis/Räder/Turm/Waffe) ist sichtbar, Kamera schräg von oben auf den Panzer.
  - WASD bewegt den Panzer, Boden „scrollt" mit (kein Nachladen-Ruckeln, kein Aufblitzen), Maus dreht den Turm.
  - Linksklick erzeugt ein gelbes Projektil-Mesh, das nach ~3 s verschwindet; Konsole zeigt `[debug][main] fired ...` und alle ~60 Frames `[debug][main] pool state { active, visible, match:true }`.
  - Konsole zeigt **kein** doppeltes Loop-Logging und keine Flacker-/Doppel-Render-Artefakte (genau ein `[info][main] loop running`).

- [ ] **Step 5: Commit** —
```bash
cd ChaosTankNew
git add src/main.ts
git commit -m "feat(bootstrap): start-screen + single game loop wiring (main.ts)"
```

---

### Task 16: Abnahme-Checkliste Slice 1a (Spec §21.1 / §21.5 / §21.6 / §21.8)

**Files:**
- Create: `ChaosTankNew/src/abnahme/abnahme1a.test.ts`

> Scope: NUR die für 1a relevanten Kriterien. **Ausgeschlossen** (1b): §21.2 Motiv-Differenz, §21.3 Promotion/Reveal, §21.4 Wiedererkennung, §21.7 Multi-Roster-Render, sowie HUD/Minimap. §21.1 und §21.8 enthalten Render-/Sicht-Anteile, die **nicht** unit-testbar sind — diese sind als **manuelle Beobachtungs-Checks** ausgewiesen; der automatisierbare Kern (getrennte Mesh-Knoten, Laufzeit-Tausch, Pool-Vertrag, Negativtest) läuft als Vitest.

- [ ] **Step 1: Failing Abnahme-Test schreiben** — `src/abnahme/abnahme1a.test.ts` mit den automatisierbaren §21-Checks. Vollständiger Code (nutzt NullEngine für headless Babylon der §21.1-Knoten-Checks):
```ts
import { describe, it, expect, vi } from 'vitest';
import { NullEngine, Scene } from '@babylonjs/core';
import { createTankView } from '../tank/tankFactory';
import { SOCKETS } from '../tank/sockets';
import type { SocketName, TankComposition } from '../tank/sockets';
import { createProjectilePool } from '../combat/projectilePool';
import { getBiome } from '../world/biomeRegistry';

function makeScene(): Scene {
  const engine = new NullEngine();
  return new Scene(engine);
}

const baseComp: TankComposition = {
  chassis: 'c_box',
  wheels: 'w_round',
  turret: 't_small',
  weapon: 'g_short',
};

const VARIANTS: Record<SocketName, [string, string]> = {
  chassis: ['c_box', 'c_wide'],
  wheels: ['w_round', 'w_tread'],
  turret: ['t_small', 't_big'],
  weapon: ['g_short', 'g_long'],
};

describe('§21.1 Modulare Teile — getrennte Mesh-Knoten auf benannten Sockets + Laufzeit-Tausch', () => {
  it('jeder Socket existiert als benannter Kind-TransformNode socket_<name>', () => {
    const scene = makeScene();
    const view = createTankView(scene, baseComp);
    for (const s of SOCKETS) {
      const node = view.root.getChildTransformNodes(false).find((n) => n.name === `socket_${s}`);
      expect(node, `socket_${s} muss als getrennter Knoten existieren`).toBeDefined();
    }
  });

  it('jeder Socket hat >=2 Varianten und setVariant tauscht das Mesh zur Laufzeit (altes weg, neues da)', () => {
    const scene = makeScene();
    const view = createTankView(scene, baseComp);
    for (const s of SOCKETS) {
      const [, v2] = VARIANTS[s];
      const before = scene.meshes.map((m) => m.uniqueId);
      view.setVariant(s, v2);
      const after = scene.meshes.map((m) => m.uniqueId);
      // Mesh-Set muss sich geaendert haben (Tausch = altes dispose + neues add)
      expect(after, `Variantentausch auf ${s} muss Szenen-Meshes veraendern`).not.toEqual(before);
    }
  });
});

describe('§21.5 Pool-Vertrag — acquire setzt alle Felder, aktiv==sichtbar', () => {
  it('acquire setzt ALLE Felder neu (kein Leak), state=inflight', () => {
    const pool = createProjectilePool(4);
    const p = pool.acquire({ x: 1, y: 2, z: 3, dx: 1, dz: 0, speed: 10, life: 2 });
    expect(p).not.toBeNull();
    expect(p!.state).toBe('inflight');
    expect({ x: p!.x, y: p!.y, z: p!.z, dx: p!.dx, dz: p!.dz, speed: p!.speed, life: p!.life })
      .toEqual({ x: 1, y: 2, z: 3, dx: 1, dz: 0, speed: 10, life: 2 });
  });

  it('activeCount entspricht der Anzahl per forEachActive iterierter inflight-Projektile (aktiv==sichtbar-Quelle)', () => {
    const pool = createProjectilePool(8);
    pool.acquire({ x: 0, y: 0, z: 0, dx: 1, dz: 0, speed: 5, life: 1 });
    pool.acquire({ x: 0, y: 0, z: 0, dx: 0, dz: 1, speed: 5, life: 1 });
    let counted = 0;
    pool.forEachActive(() => counted++);
    expect(counted).toBe(pool.activeCount());
    expect(pool.activeCount()).toBe(2);
  });

  it('nach Ablauf der Lebenszeit wird recycelt: activeCount sinkt, kein Phantom', () => {
    const pool = createProjectilePool(4);
    pool.acquire({ x: 0, y: 0, z: 0, dx: 1, dz: 0, speed: 5, life: 0.5 });
    expect(pool.activeCount()).toBe(1);
    pool.update(1); // life 0.5 -> consumed -> inactive
    expect(pool.activeCount()).toBe(0);
    let counted = 0;
    pool.forEachActive(() => counted++);
    expect(counted).toBe(0);
  });
});

describe('§21.6 Keine stillen Fallbacks — unbekanntes Biom wirft', () => {
  it('getBiome mit unbekannter id wirft erwarteten Error', () => {
    expect(() => getBiome('does_not_exist')).toThrowError('Unknown biome: does_not_exist');
  });
});

describe('§21.8 Logging liefert gestuftes Signal (Gate + Routing)', () => {
  it('scharfes Logging (enabled, minLevel debug) routet auf console und gibt Signal', async () => {
    vi.resetModules();
    const { createLogger, logConfig } = await import('../core/log');
    logConfig.enabled = true;
    logConfig.minLevel = 'debug';
    const spy = vi.spyOn(console, 'debug').mockImplementation(() => {});
    createLogger('abnahme').debug('signal');
    expect(spy).toHaveBeenCalledTimes(1);
    spy.mockRestore();
  });

  it('minLevel=warn unterdrueckt debug-Signal (gestuft, kein Spam)', async () => {
    vi.resetModules();
    const { createLogger, logConfig } = await import('../core/log');
    logConfig.enabled = true;
    logConfig.minLevel = 'warn';
    const spy = vi.spyOn(console, 'debug').mockImplementation(() => {});
    createLogger('abnahme').debug('should be muted');
    expect(spy).not.toHaveBeenCalled();
    spy.mockRestore();
  });
});
```

- [ ] **Step 2: Test laufen lassen — Expected: PASS (bestehende Module) bzw. FAIL bei Vertragsbruch.**
```bash
cd ChaosTankNew && npx vitest run src/abnahme/abnahme1a.test.ts
```
  Erwartung: Alle automatisierbaren §21-Checks (1.1 Knoten+Tausch, 5 Pool-Vertrag, 6 Negativ-Biom, 8 Logging) sind GRÜN. Schlägt einer fehl, ist das betroffene Schnittstellen-Modul vertragswidrig (echter Abnahme-Befund, NICHT „Test reparieren").

- [ ] **Step 3: Manuelle Abnahme-Checks dokumentieren & ausführen** (Render-/Sicht-Anteile von §21.1 und §21.8, die nicht unit-testbar sind). Per `npm run dev` beobachten:
  - **§21.1 (Sicht):** Per Konsole `tank.view.setVariant('chassis','c_wide')` (bzw. je Socket die 2. Variante) auslösen → die Form/Größe des Teils ändert sich **sichtbar zur Laufzeit**, restlicher Panzer bleibt stehen.
  - **§21.5 (Log-Beleg aktiv==sichtbar):** mehrfach feuern → Konsole zeigt periodisch `pool state { active, visible, match:true }`; Anzahl gelber Meshes auf dem Schirm == `active`.
  - **§21.8 (kein Doppel-Loop / kein Flackern):** genau ein `[info][main] loop running`; FPS-Anzeige stabil; keine flackernden/doppelten Projektile oder Boden-Tiles.
  - **§21.6 (Negativ, live optional):** Aufruf von `getBiome('foo')` in der Konsole wirft sichtbar `Unknown biome: foo` (kein stiller Fallback auf 'steppe').

- [ ] **Step 4: Commit** —
```bash
cd ChaosTankNew
git add src/abnahme/abnahme1a.test.ts
git commit -m "test(abnahme): slice 1a acceptance checks for §21.1/.5/.6/.8"
```

---

Plan-Hinweise (load-bearing Entscheidungen):
- **`projectileView` war im Loop-Vertrag referenziert, aber ohne formale Schnittstelle** in den Bindungen. Ich habe es als minimales Babylon-Brückenmodul `src/combat/projectileView.ts` mit `sync()` + `visibleCount()` definiert, weil §21.5 („aktiv == sichtbar") genau diese physische 1:1-Bindung Pool↔Mesh fordert. `visibleCount()` ist der direkte Messpunkt für den Log-Abgleich.
- **`startLoop` wird in `main.ts` per dynamischem `import('./core/loop')` geladen**, um die Vertragsgarantie „GENAU EIN startLoop" syntaktisch offensichtlich zu halten (ein einziger Aufrufort) — funktional identisch zum statischen Import.
- **§21.1 Mesh-Knoten-Nachweis** nutzt `scene.getChildTransformNodes`/`socket_<name>`-Namen exakt aus dem `tankFactory`-Vertrag; der Laufzeit-Tausch wird über das Mesh-Set der Szene (uniqueId-Diff) verifiziert, da `setVariant` altes Mesh disposed und neues anlegt.
- **§21.2/.3/.4/.7 und HUD/Minimap sind bewusst ausgelassen** (1b-Inhalte), wie gefordert.

Relevante Pfade: `D:\Workspace_aktuall\ChaosTankNew\src\main.ts`, `D:\Workspace_aktuall\ChaosTankNew\src\combat\projectileView.ts`, `D:\Workspace_aktuall\ChaosTankNew\src\abnahme\abnahme1a.test.ts`, Spec §21 in `D:\Workspace_aktuall\ChaosTankNew\docs\superpowers\specs\2026-06-18-chaostank-neu-design.md`.
